export const domain = 'clikstaging.herokuapp.com';
export const STAGING_URI = `https://${domain}/graphql`;
export const UPLOAD_URI = `https://${domain}/uploads/public`;
export const VIEW_UPLOAD_URI = `https://${domain}/uploads/`;
export const REST_URI = `https://${domain}`;
